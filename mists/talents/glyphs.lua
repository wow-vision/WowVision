local module = WowVision.base.windows.talents:createModule("glyphs")
local L = module.L
module:setLabel(L["Glyphs"])

-- The glyphs page is rendered by the talents window's graph screen; still a
-- placeholder, as it was on the old framework.
